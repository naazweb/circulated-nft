## Setup

1. Install dependencies

   ```bash
   npm install
   ```

## Running your app locally

1. Deploy Contract:
   `npx hardhat run scripts\deploy.js --network rinkeby`

2. Start React APP
   `npm start`
